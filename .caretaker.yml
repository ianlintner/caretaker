# Caretaker configuration for this repository.
# https://github.com/ianlintner/caretaker

memory_store:
  enabled: true
  backend: mongo # use Cosmos DB for MongoDB (set via MONGODB_URL secret)

mongo:
  enabled: true
  mongodb_url_env: MONGODB_URL # GitHub Actions secret → Cosmos DB for MongoDB
  database_name: caretaker
  memory_collection: agent_memory
  audit_collection: audit_log

# Triage subsystem (v0.11.0) — start in dry-run so the first live invocations
# surface their plan in the run report without actually closing anything.
# Flip dry_run to false once a scheduled run confirms the plan looks right.
triage:
  dry_run: true
