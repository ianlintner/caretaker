apiVersion: v1
kind: Service
metadata:
  name: caretaker-mcp
  labels:
    app: caretaker-mcp
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: caretaker-mcp
